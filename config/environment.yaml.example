# cilium config
bgp_policy:
local_asn:
peer_address_cidr:
peer_asn:
pool_block_start:
pool_block_stop:

# cert-manager config
email:
hosted_zone_id:
production_dns_zone:
staging_dns_zone:

# argocd config
argocd_server_fqdn:
github_client_id:
github_client_secret:
github_org_name:
github_org_team_name:

# demo app config
demo_app_hostname:
demo_app_staging_hostname:
